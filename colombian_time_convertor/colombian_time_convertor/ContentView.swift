//
//  ContentView.swift
//  colombian_time_convertor
//
//  Created by Henry Tran on 7/6/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.orange)
            Text("Colombian Time Convertor").padding().foregroundColor(.orange).imageScale(.small)
        }
        .padding()
    }
}
























#Preview {
    ContentView()
}
